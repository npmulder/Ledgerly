# Isle of Man Rules Pack 1.0

Versioned Isle of Man rules pack data for `isle-of-man@1.0`.

Money amounts in `pack.yaml` are integer GBP minor units (pence). Rates are
quoted decimal strings such as `"0.20"` so compliance data is not decoded
through binary floating point.
